# telemapp
---

### How to use

 - run the `run_telemetry.sh` script from the root directory of the project to compile and upload the arduino firmware and start the docker container

 - open `http://localhost:5000` to access the UI